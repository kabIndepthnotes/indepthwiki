# strlen
- find the length of a string, upto but not including the null character.

~~~{.c .numberLines}
int len;

len = strlen("abc"); /* len is now 3 */
len = strlen(""); /* len is now 0 */
strcpy(str1, "abc");
len = strlen(str1); /* len is now 3 */
~~~

# standard
[wood](wood___20250527_1447_59.pd)

# related

